git add . -then hit enter
git commit -m "text to describe what you did" -then hit enter
git push origin master -then hit enter
THEN COMPLETE HURRAH
you should now see your files on github

-----
if after git commmit you have something that says "tell me who you are"
git init
git config user.name "valeriekeolam"
git config user.email "valeriekeolam@gmail.com"
THEN do the stuff above
